# Changesets

This folder stores changeset files for version management in this monorepo.

When making changes, create a new changeset by running:

```bash
npm run changeset
```

This will guide you through selecting which packages changed and what type of change it is (major, minor, or patch).
